# nodeJS
```javascript
router.get('/selectPage', (req, res) => {
    try {
        res.render('select', { rows: [] });
    } catch (error) {
        console.error("페이지 렌더링 실패", error);
        res.status(500).send('<h1>Internal Server Error</h1>');
    }
});

router.get('/delete', async (req, res) => {
    console.log(req.query.id);

    const sql = "DELETE FROM member WHERE id = ?";

    try {
        const [rows] = await pool.query(sql, [req.query.id]);
        console.log("DB 조회 완료");
        res.redirect('/select');
    } catch (error) {
        console.error("DB 쿼리 실패", error);
        res.status(500).send('<h1>Internal Server Error</h1>');
    }
});
```
> user.js

```HTML
<body>
    <h1>HOME 페이지 입니다.</h1>
    
    로그인
    <a href="/selectPage">회원정보조회</a>

</body>
```
> home.ejs

```HTML
<body>
    <a href="/select">회원정보 조회</a>
    <table border="1">
        <thead>
            <tr>
                <th>ID</th>
                <th>Password</th>
                <th>Nickname</th>
            </tr>
        </thead>
        <tbody>
            <% if (rows && rows.length > 0) { %>
                <% rows.forEach(function(member) { %>
                    <tr>
                        <td><%= member.id %></td>
                        <td><%= member.pw %></td>
                        <td><%= member.nick %></td>
                        <td>
                            <a href="/delete?id=<%=member.id%>">삭제</a>
                        </td>
                    </tr>
                <% }); %>
            <% } else { %>
                <tr><td colspan="3">조회된 회원이 없습니다.</td></tr>
            <% } %>
        </tbody>
    </table>
</body>
```
> select.ejs
